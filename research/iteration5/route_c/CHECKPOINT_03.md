# Checkpoint 3: final blocker isolated, validations passing

Local checkpoint time: `2026-08-21 21:32 +08:00`.

The general third-level inequality remains open and has been reduced to the
single explicit state inequality

```
L_3(A)>=(2mu/m)A^-1.
```

The state is exact up to a displayed PSD Bellman residual, holds on the full
compound-correlation interval, and survived 17,920 hostile cases.  The
strongest generic shallow compression currently available fails for every
dimension `m>3` on the all-positive rank-one boundary, with exact gap
`-(m-3)/[2m(m-1)]`.  This is the crisp unresolved obstruction.

All five exact route barriers and all independent Route-A audits have been
written to portable artifacts.  The new general near-identity prefix
corollary was also checked: at `n(1-mu)<=2-sqrt(2)`, the direct WOR prefix
argument gives `H_ceil(n/2)>=mu B^-1/4`, distinct from the full-epoch
certificate.

All exact validation scripts and syntax/whitespace checks pass.  Final
timing will be recorded only after the mandated `21:37:57 +08:00` threshold.
