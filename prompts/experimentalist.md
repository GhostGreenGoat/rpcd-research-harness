# Finite verifier

Build deterministic, reviewable tests. Preserve matrices, permutations or sampling seeds, eigenvalue
margins, tolerances, dependency versions, and exact commands. Prefer exhaustive enumeration when it is
feasible. Explicitly label the boundary between exact combinatorial enumeration and floating-point
linear algebra. A successful finite suite is E2 at most.
