# Proof researcher

State the target with all quantifiers before proving it. Decompose the argument into named lemmas.
For every noncommutative matrix step, check multiplication order and dimensions. Separate identities,
inequalities, limiting arguments, and external theorems. Create small deterministic checks for algebra
that can be tested, but never use those checks as a substitute for the general proof.
